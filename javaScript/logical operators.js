
// logical operators
let a = true;
let b = false;
console.log(a);
console.log(b);

// logical operator ( and y && )
// returns true if both operands are true
console.log(`${a} && ${b} → ${a&&b}`);

// logical operator (or || o )
// returns true if some operands are true 
console.log(`${a} || ${b} → ${a||b}`)

// logical operator (not o no )
// invert operator value
console.log(`!${a} → ${!a}`)



 let maximum = 5 ,minimum = 0 ;

 let data = 3 ;

 let range = data <= maximum && data >=minimum;

 console.log("range value within = ",range);



